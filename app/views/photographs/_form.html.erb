<%= simple_form_for(@photograph) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
  <%= f.input :photo %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
